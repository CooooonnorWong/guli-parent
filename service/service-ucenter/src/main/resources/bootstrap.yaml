spring:
  application:
    name: guli-ucenter
  cloud:
    nacos:
      config:
        namespace: 5190192c-2e13-4723-9a3f-1c06f3a91abf
        server-addr: 192.168.6.100:80
        file-extension: yaml
        extension-configs:
          - dataId: jdbc-ucenter.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: jackson-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: nacos-discovery-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: redis-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: ribbon-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: feign-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: rabbitmq-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: wxpay-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: wxopen-config.yaml
            refresh: true
            group: DEFAULT_GROUP
          - dataId: common-logging-level.yaml
            refresh: true
            group: DEFAULT_GROUP
